CREATE TABLE arbeidsgiver
(
    sykmelding_id        VARCHAR(64)   NOT NULL,
    orgnummer            VARCHAR(64)   NOT NULL,
    juridisk_orgnummer   VARCHAR(64),
    navn                 VARCHAR(255)  NOT NULL
);
