version: '3'
services:
  db:
    image: postgres:11
    ports:
    - "5432:5432"
    environment:
    - POSTGRES_DB=syfosmregister
    - POSTGRES_USER=syfosmregister-admin
    - POSTGRES_PASSWORD=testpassword123
